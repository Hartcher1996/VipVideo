/**
 * 站点全局配置文件
 *
 * 前后端共享同一份：
 *   - 后端 server.js ：require('./config/site.config')
 *   - 前端         ：通过 GET /api/site/config 读取（见 server.js 对应接口）
 *
 * 修改后刷新页面即可生效（开发服务器通常自动热更新，正式部署需重启）。
 */

module.exports = {
  /**
   * 站点名称（显示在 Logo、HTML 标题、面包屑首页等位置）
   */
  siteName: 'VIP视频解析',

  /**
   * 站点副标题 / Hero 区 slogan（首页主标题下方的一句话介绍）
   */
  siteSlogan: 'VIP视频聚合搜索引擎 · 电影 / 电视剧 / 动漫 / 综艺一网打尽 · 多源智能切换',

  /**
   * 首页 Hero 区主标题（可留空使用默认：`想看的片，30秒直达播放源`）
   * 支持在文本中用 <hl>xxx</hl> 标记高亮（会被替换为带主题渐变色的 span）
   */
  heroTitle: '想看的片，<hl>30秒直达播放源</hl>',

  /**
   * 首页 Hero 区搜索框 placeholder
   */
  searchPlaceholder: '搜索电影、电视剧、动漫、演员、导演...',

  /**
   * 首页「大家都在搜」热搜词条（至少 1 条，建议 6~10 条）
   * 点击后自动填入搜索框并执行搜索；
   * 想清空的话写 []，首页不会显示热搜区域。
   */
  hotKeywords: [
    '星际穿越',
    '繁花',
    '葬送的芙莉莲',
    '奥本海默',
    '庆余年 第二季',
    '狂飙',
    '三体',
    '长津湖',
    '鬼灭之刃',
    '间谍过家家'
  ],

  /**
   * 播放页默认是否开启「宽屏模式」（隐藏右侧剧集列表）
   *   true  => 进入播放页默认宽屏（更沉浸，适合大屏）
   *   false => 进入播放页显示剧集列表（默认，方便选集）
   */
  defaultWideScreen: false,

  /**
   * 首页底部 Stats 数据条（纯展示，可自定义文案，留空数组则不显示）
   */
  stats: [
    { num: '100+', label: '全网资源聚合' },
    { num: '<200ms', label: '毫秒级搜索响应' },
    { num: '4路+', label: '多播放源备份' },
    { num: '0强制', label: '无贴片无会员弹窗' }
  ],

  /**
   * 页脚版权信息（可留空）
   */
  footer: '© 2026 VIP视频解析 - 仅供学习交流使用'
};
